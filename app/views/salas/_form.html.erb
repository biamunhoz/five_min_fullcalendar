<%= form_with(model: sala, local: true) do |form| %>
  <% if sala.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sala.errors.count, "error") %> prohibited this sala from being saved:</h2>

      <ul>
      <% sala.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :cor %>
    <%= form.text_field :cor %>
  </div>

  <div class="field">
    <%= form.label :permissaoauto %>
    <%= form.check_box :permissaoauto %>
  </div>

  <div class="field">
    <%= form.label :observacao %>
    <%= form.text_area :observacao %>
  </div>

  <div class="field">
    <%= form.label :confirmacao %>
    <%= form.check_box :confirmacao %>
  </div>

  <div class="field">
    <%= form.label :agenda_id %>
    <%= form.text_field :agenda_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
